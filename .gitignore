node_modules/
dist/
.turbo/
*.log
*.local
.env
.env.*
!.env.example
.DS_Store

# local sqlite databases
*.db
*.db-shm
*.db-wal

# build / deploy
.wrangler/
.vite/
# Local blob-storage dir used by the Node/fs StorageAdapter at runtime. Scoped to
# the app dir so it does NOT match source dirs named `storage/` (e.g.
# packages/core/src/storage/, which is the StorageAdapter source — must be tracked).
/apps/api/storage/
