{
  "name": "@lawlink/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply lawlink-db --local",
    "d1:migrate:remote": "wrangler d1 migrations apply lawlink-db --remote"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@lawlink/core": "workspace:*",
    "@lawlink/db": "workspace:*",
    "hono": "^4.6.14",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "wrangler": "^4.105.0"
  }
}
