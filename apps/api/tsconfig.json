{
  "extends": "../../tsconfig.base.json",
  // The Hono app (index.ts) is the single entry for BOTH runtimes: Node via
  // server.ts, and Cloudflare via wrangler (esbuild bundles index.ts for the
  // Worker — there is no separate Worker source/program). Typechecked once here
  // under Node; Cloudflare bindings reach it through the typed `c.env` (Bindings).
  "include": ["src"]
}
